---
title: Introduction
description: The content studio built for agents.
---

![open-frame editor showing a frame with the inspector and assets panel docked alongside the canvas](/assets/screenshots/open-frame-cover.webp)

**open-frame** turns React components into slides, carousels, stories,
thumbnails, and OG images. You describe what you want in natural language; your
coding agent writes the React. open-frame handles the canvas, scaling,
navigation, hot reload, present mode, and export so the agent can focus on
content.

A **frame** is one piece of content — a talk, a carousel, a single image. It
lives in `frames/<id>/` as a React module exporting one or more pages, and each
page renders into a fixed **1920 × 1080** canvas. Frames are arbitrary React
components — not a constrained DSL.

```npm
npx @open-frame/cli init
```

## Why open-frame

Visual content is just code. Agents are great at writing code. open-frame is the
missing runtime that turns *"make slides about X"* into something polished and
presentable — without you ever leaving the chat.

- **Agent-native authoring.** Ships with skills (`/create-frame`,
  `/frame-authoring`, `/apply-comments`, `/create-theme`) that any
  Claude Code, Codex, or Cursor session can call.
- **In-browser inspector.** Click any element in the dev server, drop a
  `@frame-comment`, and your agent applies it on the next `/apply-comments`.
- **Assets manager + svgl.** Drop in images, search 1500+ brand logos through
  the integrated [svgl](https://svgl.app/) catalogue.
- **Professional present mode.** Fullscreen playback, presenter view, speaker
  notes, timer.
- **Export to static HTML or PDF.** Self-contained output — share without a
  server.

## A frame is a file

A frame is a React component. The runtime renders one component at a time
onto a fixed 1920×1080 canvas, scales it to fit the viewport, and hands you
the navigation, hot reload, and present mode.

Each frame lives under `frames/<kebab-case-id>/`. The entry is
`frames/<id>/index.tsx`. Assets sit alongside in `frames/<id>/assets/`.

```tsx title="frames/getting-started/index.tsx"
import type { Page, FrameMeta } from '@open-frame/core';

const Cover: Page = () => (
  <div style={{ width: '100%', height: '100%', background: '#08090a', color: '#f7f8f8' }}>
    <h1 style={{ fontSize: 188, letterSpacing: '-0.04em' }}>
      Hello, <em style={{ color: '#7170ff' }}>open-frame</em>.
    </h1>
  </div>
);

export const meta: FrameMeta = { title: 'Getting started', theme: 'corporate' };
export default [Cover] satisfies Page[];
```

Two rules: the default export is an array of components (one per page), and
each component fills its container (`width: '100%'` / `height: '100%'`). No
DSL, no markdown chain — if you can build it in React, it's a frame.

## The 1920 × 1080 canvas

Every page renders into a fixed 1920×1080 box. The runtime scales that box
uniformly to fit the viewport, so the same frame looks identical on a phone,
a 4K monitor, and a beamer.

Fixed canvas + uniform scaling means every pixel coordinate you write is the
coordinate that ships — preview, stage, and exported PDF stay in lockstep.
The constants are exported if you need them:

```ts
import { CANVAS_WIDTH, CANVAS_HEIGHT } from '@open-frame/core';
// 1920 × 1080
```

Inside a page, two layout patterns work well: **absolute coordinates**
(`position: absolute` + pixel values — predictable, easy for an agent to
write) and **flex/grid centring** for content that flows. Avoid responsive
breakpoints; the canvas is fixed. Container queries (`containerType:
'inline-size'` + `cqw` units) are a useful middle ground for reusable
widgets.

## What's next

<Cards>
  <Card title="Get started" href="/docs/getting-started" />
  <Card title="Create a frame" href="/docs/flow/create-frame" />
  <Card title="Inspector & comments" href="/docs/core-feature/inspector" />
</Cards>
