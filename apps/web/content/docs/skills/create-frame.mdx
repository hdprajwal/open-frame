---
title: /create-frame
description: Drafts a deck end-to-end from a one-line prompt.
---

`/create-frame` is the entry-point skill for any new deck. From a one-line
prompt to a polished outline in a single skill call.

## What it does

1. **Scoping** — asks four questions: topic & aesthetic, page count, text
   density, motion vs. static.
2. **Theme lookup** — if a theme is referenced, reads `themes/<id>.md`
   first so the deck inherits the design system. See
   [Themes](/docs/core-feature/themes).
3. **ID & structure** — picks a kebab-case id, plans the page list.
4. **Authoring** — writes `frames/<id>/index.tsx` page by page, deferring
   to [`/frame-authoring`](/docs/skills/frame-authoring) for the *how*.

## When to use it

For any new deck. Even if you plan to hand-author, kick off with
`/create-frame` to seed the folder, the page array, and the meta block.

```text
/create-frame for "Q2 launch — 3 chapters, mixed text density, subtle motion, dark aesthetic"
```

## What you get

A working `frames/<id>/index.tsx` with one component per page, a `meta`
export, and assets imported from `frames/<id>/assets/`. Drop into the dev
server and iterate from there — see [Iterate](/docs/flow/iterate).
