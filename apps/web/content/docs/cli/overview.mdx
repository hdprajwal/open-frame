---
title: Overview
description: Two CLIs — one to scaffold, one to run.
---

open-frame ships two CLIs:

- **`@open-frame/cli`** — the scaffolder. One command (`init`) to bootstrap
  a new workspace.
- **`@open-frame/core`** — the runtime CLI. `dev`, `build`, `preview`, and
  `sync:skills` for an existing workspace.

After `init`, `package.json` exposes the runtime CLI under standard scripts:

```json
{
  "scripts": {
    "dev": "open-frame dev",
    "build": "open-frame build",
    "preview": "open-frame preview",
    "sync:skills": "open-frame sync:skills"
  }
}
```

So in day-to-day use you'll just type `npm run dev`, `npm run build`, etc.
