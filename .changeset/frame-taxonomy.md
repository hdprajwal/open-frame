---
"@open-frame/core": major
"@open-frame/cli": major
---

Rename the slide taxonomy to frame across the public API, config (`framesDir`), content directory (`frames/`), CSS variables (`--of-*`), routes (`/f/:frameId`), comment markers (`@frame-comment`), and the bundled skills.
