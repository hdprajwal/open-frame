---
'@open-slide/core': minor
---

Add a Global assets manager. Files in the project root `assets/` folder can be imported via the `@assets/*` Vite alias and are reusable across decks and themes. The dev-mode UI exposes it two ways: a Slide / Global scope toggle inside any slide's Assets view, and a dedicated Assets entry in the home sidebar at `/assets`.
